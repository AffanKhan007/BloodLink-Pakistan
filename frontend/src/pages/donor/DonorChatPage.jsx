import ChatWorkspace from "../../components/ChatWorkspace";

export default function DonorChatPage() {
  return (
    <ChatWorkspace
      eyebrow="Donor messaging"
      title="Receiver conversations"
      description="Reply to request conversations that were opened through a valid match or public availability connection."
    />
  );
}
